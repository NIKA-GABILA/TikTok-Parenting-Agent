# Core dependencies
anthropic>=0.40.0
python-telegram-bot>=20.7
pillow>=10.0.0
pytz>=2023.3
apscheduler>=3.10.4

# Web server for health check (Render compatibility)
aiohttp>=3.9.0

# Optional but recommended
python-dotenv>=1.0.0
